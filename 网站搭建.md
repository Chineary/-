# 港大 AI 导师网站搭建计划

## 1. 页面与数据约定

本机运行，读取《AGENTS.md》流程产出的文件。顶部筛选导师，左侧切换“基础信息、研究方向、合作者”，右侧展示详情。

以下文件位置统一约定：
- **导师总表**：`港大/导师信息汇总.csv`。
- **导师个人文件**：`港大/【导师姓名】/`，下文使用相对文件名。其中，`【导师姓名】` 使用第一步 `./港大/导师信息汇总.csv` 中目标导师的“姓名”，不替换为合作者姓名。
- 论文通过“导师 + 序”关联；姓名翻译只影响显示，不改变目录名和关联标识。

## 2. 基础信息与顶部筛选

以下信息均来自**第一步的导师信息汇总.csv**：

- **导师搜索与选择** ← `姓名、中文名`；优先显示中文名，同时支持中英文搜索。
- **院系、方向、博士招生筛选** ← `院系、研究兴趣和方向、是否招收PhD`。方向标签从该字段统一整理，不混用潜在方向。
- **身份信息** ← `院系、职称、岗位类型（研究型/教学型/礼聘等）、是否研究型教师、院内及联聘职务`。
- **主页入口** ← `学校主页、个人主页、Google Scholar网址、CV网址、ORCID网址、DBLP网址`。
- **总引用数及统计日期** ← `Google Scholar总被引次数、引用统计日期`；这两个字段由第二步 2.1 回填，来源为 `Google Scholar网址`。
- **教育与工作经历** ← `教育经历（本硕博院校信息及获取学位时间）、任职`；包括企业经历，保留原有时间和工作内容。
- **研究兴趣、实验室、招生情况** ← `研究兴趣和方向、实验室/课题组名称及网址、是否招收PhD、招生人数及招生方向`。
- **资料来源与更新时间** ← `资料来源网址、抓取日期、核验状态`。

## 3. 研究方向

- **主题解释、阶段时间线、历史方向、方向变化、当前方向、潜在方向** ← 第四步 `研究轨迹分析.md` 中的“主题清单、教育任职时间线、分阶段方向、方向变化、当前方向、潜在方向”各章节。直接展示已有分析，保留“推断、公开计划、待核验”等标记。
- **年度论文数量、主题分布、阶段分布图** ← 第四步 `论文研究时间线.csv` 的 `序、年份、统一研究主题、论文对应经历阶段`。统计规则沿用第四步，版本待核验记录不计数，未分类记录单列。
- **论文筛选** ← 年份、主题取自时间线；发表／预印本取自第三步 `论文/论文信息汇总.csv` 的 `文章阶段（发表/预印本）`。
- **论文详情** ← 第三步论文汇总的 `文章标题、年份、摘要、关键词、研究方向、研究内容、创新点、是否为一作/通讯、被引次数、引用统计日期、引用来源网址`。
- **作者、会议／期刊、论文链接、下载状态** ← 第二步 `论文/论文记录.csv` 的 `作者、发表载体、论文链接、下载状态`，按 `序` 关联。
- **研究结论依据** ← 时间线的 `证据位置` 和分析报告中的引用；`未完成事项序号` 对应 `未完成事项.csv` 的 `序号`，展示失败原因及下一步建议。

## 4. 合作者

以下文件均由**第五步**生成：

- **合作者列表** ← `合作者信息汇总.csv` 的 `姓名、当前单位、身份、职称、与导师的关系、共同论文数、首次合作年份、最近合作年份、共同研究方向`；默认按共同论文数排序。
- **单位变化、项目与证据** ← 同表的 `论文署名单位、当前单位、共同项目、关系证据、待核验问题`；历史署名单位与当前单位分开展示。
- **共同论文详情** ← 在该表新增 `共同论文序号` 字段，第五步 5.1 汇总时写入，用分号分隔；据此关联第三步论文汇总，避免靠姓名猜测论文关系。
- **导师末位／通讯署名比例** ← `共同论文署名表.csv` 的 `合作者姓名、论文序号、导师是否末位、导师是否通讯`；按第五步 5.4 计算，待核验项不进入对应分母，依据取 `证据位置`。
- **合作者论文及一作情况** ← `合作者论文简表.csv` 的 `合作者姓名、论文标题、年份、角色、关键词、是否与目标导师合著、来源及核验依据`。
- **身份变化、学生表现与去向、教师研究方向、合作原因、合作时间线** ← `合作分析.md` 中对应内容；第五步 5.7 保存时固定这些章节名称，供网站准确读取。

## 5. 中文显示、开发与验收

- **中文显示**：导航、字段名、筛选项、研究主题、摘要、研究内容、创新点、经历和分析说明全部使用中文。论文原题、会议／期刊名称、必要缩写保留原文；人名无可靠中文名时保留原名。
- **译文处理**：导入阶段生成并保存中文展示副本，统一专业术语；原始文件与引用证据原文保留。页面不直接展示整段英文摘要或英文分析。
- **文字和界面要求**：网站页面简单干净，不要太花哨。文字简洁、直接，先写结果和重点；使用短句，不绕弯，不用绕口或浮夸的表述。
- **技术实现**：采用 React + TypeScript + Vite，图表使用 ECharts；Node 脚本解析 CSV 和 Markdown，生成网站读取的 JSON。
- **开发顺序**：先完成字段映射与导入校验，再做基础信息，随后接入研究方向和合作者。研究报告按第四步 4.7 固定章节读取，无法识别的章节报告导入错误。
- **缺失处理**：文件尚未生成显示“尚未整理”；已有字段保留“未公开、未获取、待核验”等状态，缺失引用数不显示为 0。
- **验收**：选一位资料完整的导师，逐项核对页面与原文件；检查跨表关联、共同论文跳转、引用日期、统计数量、中文显示、筛选和窄屏布局。
- **交付**：提供本地访问地址和一条数据更新命令。当前只有导师名单，先支持名单浏览，完整数据生成后接入。
